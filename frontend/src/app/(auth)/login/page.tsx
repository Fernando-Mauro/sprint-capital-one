import type { ReactNode } from 'react';

export default function LoginPage(): ReactNode {
  return (
    <div>
      <h1>Login</h1>
      {/* TODO: Add login form with email/password and OAuth providers */}
    </div>
  );
}
