import type { ReactNode } from 'react';

export default function RegisterPage(): ReactNode {
  return (
    <div>
      <h1>Register</h1>
      {/* TODO: Add registration form with email, password, display name */}
    </div>
  );
}
