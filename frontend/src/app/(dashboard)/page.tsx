import type { ReactNode } from 'react';

export default function DashboardPage(): ReactNode {
  return (
    <div>
      <h1>Your upcoming matches</h1>
      {/* TODO: Fetch and display user's upcoming matches */}
    </div>
  );
}
