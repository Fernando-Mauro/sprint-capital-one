import type { ReactNode } from 'react';

export default function BrowseMatchesPage(): ReactNode {
  return (
    <div>
      <h1>Browse Matches</h1>
      {/* TODO: Add search/filter controls and match listing */}
    </div>
  );
}
