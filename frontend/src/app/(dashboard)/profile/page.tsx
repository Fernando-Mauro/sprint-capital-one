import type { ReactNode } from 'react';

export default function ProfilePage(): ReactNode {
  return (
    <div>
      <h1>Your Profile</h1>
      {/* TODO: Display and edit user profile (avatar, display name, bio, location) */}
    </div>
  );
}
