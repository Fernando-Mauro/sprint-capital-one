import type { ReactNode } from 'react';

export default function HomePage(): ReactNode {
  return (
    <div>
      <h1>MatchUp — Find your next game</h1>
      {/* TODO: Add hero section, featured matches, CTA */}
    </div>
  );
}
